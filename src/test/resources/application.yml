# application.yml에 아무것도 없어도 기본적으로 spring boot에서 test 메모리 모드를 제공해준다.

spring:
#  datasource:
#    url: jdbc:h2:mem:test # Spring boot에서 메모리 모드를 제공해준다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties: # hibernate의 특정 properties를 설정
#      hibernate:
#        show_sql: true # sout을 통해서 출력, 따라서 사용을 권장하지 않음
#        format_sql: true

logging:
  level:
    org.hibernate.sql: debug # hibernate SQL 로그를 debug 모드로 사용 // logger를 통해서 출력
#    org.hibernate.type: trace